apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "redpay-site.fullname" . }}-test-connection"
  labels:
    {{- include "redpay-site.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "redpay-site.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
