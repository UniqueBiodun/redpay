apiVersion: v1
kind: Service
metadata:
  name: {{ include "redpay-site.fullname" . }}
  labels:
    {{- include "redpay-site.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "redpay-site.selectorLabels" . | nindent 4 }}
